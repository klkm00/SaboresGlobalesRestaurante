server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: sabores-globales-clave-secreta-super-segura-2024
  expiration: 86400000